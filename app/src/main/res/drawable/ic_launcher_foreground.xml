<?xml version="1.0" encoding="utf-8"?>
<!-- A bold, geometric "L" wordmark, kept inside the adaptive-icon safe zone
     (roughly the inner 66dp of the 108dp canvas). Accent is
     LedgerV3Palette.Azure (#3B82F6) — reused, not invented, so the launcher
     icon and the in-app splash share the exact same accent. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#3B82F6"
        android:pathData="M40,26h14v46h20v14h-34z" />
</vector>
