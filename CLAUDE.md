# Ledger — Project Context

Offline-first Android personal-finance app. Kotlin, Jetpack Compose, Hilt, Room, MVVM+Clean Architecture. Package `com.sherif.ledger`. Work happens on `feature/ldl-foundation`.

## Build & test

- Debug build: `./gradlew installDebug`
- Release build: `./gradlew assembleRelease` — compiles a stricter, separate task graph than debug. A change that passes `compileDebugKotlin` is not proven to pass `compileReleaseKotlin`; run both before calling something done.
- Unit tests: `./gradlew testDebugUnitTest`
- No Robolectric — unit tests are plain JVM, no Android framework classes available. Anything that must be faked in a test (notifications, `Context`, etc.) needs an interface with a real Android-backed implementation, so the fake can implement the interface without touching the framework.

## Critical gotchas (each one caused a real broken build this session)

- **Same-package top-level `private object`/`private class` name collision.** `private` on a top-level declaration restricts which *file* can reference it, but Kotlin still generates a class with that exact name in the *package's* namespace regardless. Two files in the same package declaring `private object Foo` collide — "Redeclaration" / "cannot access, it is private in file" errors that look unrelated but share this one cause. If a test double is needed in more than one file in the same package, put it in its own file without `private` (use `internal`) instead of duplicating it.
- **Expression-bodied functions (`fun x(): Y = try { ... }`) cannot contain bare `return`.** Only legal in a block body (`fun x(): Y { return try { ... } }`). This specifically broke `compileReleaseKotlin` while `compileDebugKotlin` passed — don't trust a debug-only compile as proof.
- **CRLF varies per file in this repo.** Some files have Windows line endings, some don't — it's not consistent. Match whatever the specific file already uses; don't bulk-normalize across the repo (a wildcard/timestamp-based line-ending pass will silently rewrite files that were never touched).
- **Build-variant source sets already do real work here.** `app/src/debug/` and `app/src/release/` both define their own `DebugNavGraph.kt` and `RealPipelineTracker.kt` — release's are deliberate no-op stubs so debug tooling never ships to production. Before adding a "Developer Console" feature or similar, check both source sets, not just `app/src/main`, or you'll duplicate work and get a "Conflicting overloads" compile error.
- **`git log` from WSL and PowerShell against the same checkout can disagree on paths.** Backslash paths that are correct in PowerShell resolve to nothing in WSL's git. If a git command returns suspiciously empty, check which shell it ran in before concluding the history doesn't exist.
- **A fake repository's `Flow<...> = flowOf()` (zero arguments) is a truly empty flow, not "no data yet."** `DeterministicAccountIdentityResolver` calls `accountRepository.observeAllAccounts().first()` unconditionally — `.first()` on a zero-emission flow throws `NoSuchElementException` immediately. Fakes should emit `flowOf(LedgerResult.Success(emptyList()))` or a real list, never bare `flowOf()`, for any flow something might call `.first()` on.
- **Before assuming a failing test means you broke something, check whether it ever ran before.** Every compile attempt earlier this session failed before `testDebugUnitTest` could execute anything — so failures that first appeared once compilation finally succeeded could be pre-existing and simply never run until now, not new regressions. Diff the test file and the production code it exercises against `git show a9206b5:<path>` (the original pre-session base) before assuming a change caused a failure.

## Frozen / protected — don't refactor without evidence a bug exists

`BalanceCalculator.kt`, `AccountBalanceService.kt`, `GetFinancialAnalyticsUseCase.kt`, `RelationshipResolvers.kt`, `RelationshipEngine.kt`, `ReconciliationEngine.kt` (scoring specifically), `MerchantResolver.kt`, `MerchantRegistry.kt`, `PipelineTracer.kt`/`PipelineTrace.kt`, `ExtractionRegistry.kt`, `ConfirmationMatcher.kt`. These encode deliberately-tuned scoring and matching logic. Changes here need a specific, demonstrated bug, not general cleanup.

`core.common.diagnostics.PipelineTracker`/`RealPipelineTracker`/`PipelineEvent` (old system) is confirmed dead — nothing writes to it anymore — but is deliberately still present, not deleted, so `app/src/debug`'s existing references still compile. Don't build anything new on it. The live system is `feature.diagnostics.PipelineTraceSink`, eagerly populated by `ProcessNotificationUseCase` on every real capture.

**Removed** (was here, no longer exists): `feature.capture.parsing.extraction.{TransactionExtractor,AmountExtractor,MerchantExtractor,CurrencyExtractor,TransactionTypeResolver}` — a second, older, entirely unused extraction pipeline, confirmed via zero production instantiations and zero DI wiring (grepped all of `app/src/main`, including every `core/di` module). Deleted along with its two dedicated tests (`ExtractionEngineTest.kt`, `AmountExtractorTest.kt`) rather than patched, because nothing in the app called it — patching a bug nobody reaches doesn't change app behavior, and the project's own standard is no duplicated code. `TextNormalizer`, `PatternEngine`, `MerchantNormalizer`, `ExtractionHelpers` (same package) are NOT dead — those are shared by the live `AdcbParser`/`GenericBankParser`/`HeuristicExtractor` path and were left untouched.

The bug the dead `AmountExtractor` had — `Regex.find()` grabbing the *first* number in the text (e.g. "ending in **1234**" before "AED **1,250.50**") instead of the actual amount — was checked against the live path and does NOT exist there: `ExtractionHelpers.extractAmountMinor` (used by `HeuristicExtractor`, the real extractor) is currency-anchored (`AED|USD|INR|Rs...` must precede the digits) specifically to avoid this. Verified, not assumed: added a new test to `HeuristicIntentTest` — "amount extraction ignores an account digit run that precedes the currency amount" — reproducing the exact scenario against the live extractor — passes. The parallel merchant-name-truncates-on-period question (dead `MerchantExtractor` turned "Amazon.ae" into "Amazon") was never resolved for the live `HeuristicExtractor.extractMerchant` either way — no corpus fixture asserts merchant name content, so this is unverified territory on the live path, not a confirmed bug.

## Feature reachability — verified, not assumed

Code existing in the repo is not the same as a user being able to reach it. Current status:

- **Split (Ledger Split)**: domain model, database, repository all real and tested. Zero ViewModel, zero screen, zero navigation route. Not reachable by any user action.
- **Transaction Notes**: backend correct (real `note` column, working `updateNote`), but `TransactionDetailsScreen.kt` doesn't render or edit it anywhere. Not reachable.
- **Notification actions (Split/Add Note/Undo on capture)**: built and wired, but `POST_NOTIFICATIONS` is never requested at runtime. On Android 13+ this defaults to denied and stays denied without a manual Settings grant — the feature is effectively unreachable on a fresh install until a runtime permission request is added.
- **Developer Console**: reachable, confirmed end-to-end (Profile → Developer Console). Debug builds only, by design.
- **Ledger Diagnostics** (RC4): reachable from Developer Console's top bar, debug builds only. `Export Diagnostic Bundle` produces a shareable zip via a real `FileProvider`.
- **Adjust Starting Balance**: reachable, Profile → Adjust Starting Balance, all builds (2026-07-17).
- **Profile Setup** (name/email): reachable, shown automatically on very first launch before anything else (2026-07-17) — see below. **Edit Profile** (Profile screen's button, previously `onClick = { /* TODO */ }`) now reaches the same screen in edit mode (2026-07-17) — `ProfileSetupScreen(isEditMode = true)`, fields pre-filled from `ProfileSetupViewModel`'s `init` block reading current prefs.

## Added — user identity was hardcoded everywhere; local-only Profile Setup added (2026-07-17)

"Sherif Rahim" / "SR" / "sherif.rahim@gmail.com" were hardcoded in exactly two places (confirmed via grep, no others): `ProfileScreen.kt`'s `UserProfileHeader` and `DashboardScreen.kt`'s `DashboardTopBar` avatar. Explicitly scoped with the user first: **local-only** profile (name + email, no password/server/real auth — this app has no networking/auth layer anywhere today, and "real sign-in + cloud sync" was deferred as a separate future decision), shown **first**, before notification-access or SMS-import onboarding.

- New `ProfileSetupScreen`/`ProfileSetupViewModel` (`feature/onboarding/presentation`), gated by a new `UserPreferencesRepository.isProfileSetup` flag (same pattern as `isSmsImported`) and a new `MainViewModel.isProfileSetup`, checked first in `MainActivity`'s launch sequence (before permission/SMS-import checks).
- New `UserProfileViewModel` (`feature/settings/presentation/viewmodel`) is the ONE place name/email/derived-initials are read from — both `DashboardTopBar` and `ProfileScreen`'s `UserProfileHeader` inject it via `hiltViewModel()` rather than each deriving initials separately.
- New reusable `core/designsystem/component/LedgerTextField.kt` (generic free-text LDL input, same visual family as `LedgerSearchBar`/`LedgerAmountInputField`) — used for the name/email fields, not Material's `OutlinedTextField`.
- Minimal validation only, on purpose: non-blank name, email containing `@`. No format/existence validation — there's no server to validate against yet.
- **Compile gotcha hit twice this session**: explicitly importing `androidx.compose.foundation.layout.weight` breaks it — `Modifier.weight()` is a `RowScope`/`ColumnScope` extension resolved implicitly inside a `Row`/`Column` lambda; importing it as a bare top-level name causes `"Cannot access 'val RowColumnParentData?.weight: Float': it is internal in file"`. Never import `weight` explicitly; it resolves on its own inside `Row { }`/`Column { }`.

Before marking any new feature "done," confirm it end-to-end: backend → ViewModel → screen → navigation → actually tappable from somewhere a user would be.

## Resolved — Dashboard Total Balance sign bug (was "Open investigation")

Phase B happened: a real `ledger_diagnostic_*.zip` was analyzed (2026-07-17, device OPPO CPH2651). It showed the actual root cause was never the balance *arithmetic* — `database_health.json`'s `balance_reconstruction` check passed (independently-replayed balances matched `AccountBalanceService` exactly), and `financial_trace.json`/`ledger.log` showed the backend correctly computing net worth as **-AED 53,493.38**. The very next log line showed the Dashboard displaying `totalBalance=AED 53493.38` — same magnitude, **positive**. Root cause: `MoneyFormatter.format()` always does `abs(money.minorUnits)` (by design — safe for transaction amounts, which carry direction via a separate `isExpense`-style field). `DashboardUiState.totalBalance` (net worth) had no such companion field, so its sign vanished completely between `DashboardViewModel` and the UI — unlike the Accounts screen, which already threads an `isNegative` flag per account (`AccountUi.isNegative`, colored via `LedgerTheme.colors.negative` in `AccountsScreen.kt`).

Fixed: added `DashboardUiState.isNegativeBalance` (set from the raw signed total in `DashboardViewModel`, before `MoneyFormatter` drops the sign), consumed in `DashboardScreen.TotalBalanceSection` to color the balance red when negative, matching the existing Accounts-screen convention. Also fixed an adjacent bug in the same composable found from the same log line (`balanceChangePercentage=-81%`): the month-over-month change badge was unconditionally styled green/"positive" regardless of the string's actual sign.

Not fixed, not asked: `AccountsUiState.netWorth` has the identical missing-sign defect, but `AccountsScreen.kt` never actually renders that field (only `.sections` is consumed) — no live/visible bug there currently, so left alone. Worth the same fix if that field ever gets wired up.

The two previously-patched bugs (liability self-match double-count in `AccountBalanceService`, a `DeterministicAccountIdentityResolver` scoring bug) remain real fixes but are now understood to be secondary — the diagnostic evidence points at the display bug above as what users actually saw.

## Resolved — why import balloons to ~1 year of SMS history (Parts 1-3, 2026-07-17)

Confirmed in code, not just inferred from transaction count: `SmsImporter.importHistoricalSms()` queried SMS with only a lower bound (`DATE > lastImportDate`, defaulting to epoch `0` on a fresh install) and **no upper bound at all** — a first-run import scanned the device's entire SMS history, unbounded.

Fixed by building the onboarding range-selection the user actually wanted, not just capping the query:
- New first step in `SmsOnboardingScreen`/`SmsOnboardingViewModel` (`ImportRangeSelectionScreen`), shown before the existing permission/scan step: This Week (default) / This Month / Last 3 Months / Last 12 Months / Custom Date Range. New `ImportRangeOption.kt` resolves each preset to an `Instant` range; `CUSTOM` uses a new custom-built calendar, `core/designsystem/component/calendar/LedgerDateRangeCalendar.kt` (LDL-styled month grid, tap-to-set start then end — deliberately not Material's `DatePicker`/`DateRangePicker`, per AI_ENGINEERING_GUIDE.md's "Material is a rendering primitive only" rule).
- `SmsImporter.importHistoricalSms(importStartDate: Instant, importEndDate: Instant = Instant.now(), windowLabel: String = "")` now takes the window explicitly instead of hardcoding it; the existing `lastSmsImportDate` watermark still applies on top (`effectiveStart = max(importStartDate, lastImportDate)`) so a second/retry call never reprocesses already-handled messages even with a wider re-selected range.
- Investigated before building: `core/model/DateRange.kt` (dead, zero imports, same dead package already noted below) and `SearchFilterScreen.kt`'s "Date range" filter row (hardcoded mock string `"Jun 1 - Jun 25, 2025"`, `onClick = {}`, no ViewModel) were the only "date range" hits in the repo — neither was reusable. Nothing existing was thrown away; nothing was duplicated.
- `ProcessNotificationUseCase.execute()` return type changed from `Unit` to a new additive `ProcessNotificationOutcome` (filterAccepted + CREATED/MERGED/DISCARDED), purely so `SmsImporter` can tally real outcomes for diagnostics — non-breaking (Kotlin doesn't require consuming a return value; `LedgerNotificationListener`/`SmsReceiver`/all existing tests still compile untouched).
- New `UserPreferencesRepository.ImportSummary` persists the chosen window + tallies (SMS scanned/within-window/ignored/matched, transactions created/merged/discarded) so they survive past the import itself. New `ImportSummaryCollector` (`import_summary.json`) added to the exported diagnostic bundle (Part 4) — bundle-only, no live in-app diagnostics screen, per explicit choice over a broader live-UI option.
- **Now visually verified on OPPO CPH2651** (2026-07-17, second diagnostic bundle `ledger_diagnostic_20260717_1526.zip`): range selection, "This Month" import, and `import_summary.json` in the exported bundle all work as built. The Dashboard sign fix above is confirmed live too — Financial State rendered correctly in red for a real negative balance.

## Resolved — `openingBalance` is always zero; a bounded import window makes this visible (2026-07-17)

Real-world evidence from testing "This Month": Dashboard showed **-AED 8,030.88**; the user's actual bank balance was **+AED 4.3k**. Investigated before concluding anything — sampled ~20 of the run's 262 discarded messages directly from `ledger.log` (telecom low-balance nags, a Mashreq installment-offer promo, Kotak "account inactive" notices, AMB-shortfall notices, an Axis "credit balance" info message) — all genuinely non-transactional, none a missed real transaction. `database_health.json`'s `balance_reconstruction` check also still passed. This ruled out the filter/parser as the cause.

Root cause: every account-creation path (`DeterministicAccountIdentityResolver.kt:250`, `EnsureDefaultAccountUseCase.kt:30`) hardcodes `openingBalance = Money.zero(...)` — confirmed via grep, no exceptions. `Account.kt`'s own doc comment says the *design* assumes a real opening balance ("current balance is always derived by replaying transactions... starting from this opening value") but nothing had ever actually set it to anything but zero. This was tolerable when import was unbounded (the whole SMS history was replayed, so "zero, plus nearly everything" was roughly right); a bounded window (This Month here) breaks that assumption on purpose, since a chunk of real balance history now sits outside the window with no anchor to replace it. The math was self-consistent given the incomplete data (per `balance_reconstruction` passing) — a completeness gap, not an arithmetic bug.

**Fixed**: new `SeedOpeningBalanceUseCase` — reads the account's current *computed* balance (from `AccountBalanceService`, still the only source of balance truth), takes a user-supplied real balance, and persists `openingBalance = existingOpeningBalance + (actualBalance - computedBalance)` via `AccountRepository.updateAccount` (already existed, fully wired with an integrity-check audit log, zero callers before this — clearly built for exactly this and never used). Explicitly documented as a deliberate ONE-TIME-PER-USE exception to `Account.kt`'s "opening balance is never mutated afterward" rule. Two entry points, both calling the same use case:
1. "Confirm Starting Balance" step in `SmsOnboardingScreen`/`SmsOnboardingViewModel`, shown once after a successful scan (skippable, per-account, blank = no correction) — routes through `proceedPastImport()` so it's skipped entirely when there's nothing to confirm.
2. **Standalone, reachable anytime**: Profile → "Adjust Starting Balance" (`AdjustBalanceScreen`/`AdjustBalanceViewModel`, `LedgerRoute.AdjustBalance`). Added because the onboarding step only runs once per install — a user who already completed onboarding before this existed (or whose `isSmsImported` flag is already `true` from a prior test) can never see step 1 again without a full reinstall/data-clear. This was hit directly during testing: a rebuild-and-reinstall-over-existing-app cycle skipped onboarding entirely since the flag persisted, so the balance stayed wrong despite the fix being in place.

Shared, extracted (not duplicated) between both entry points: `core/domain/util/PlainDecimalAmount.kt` (`formatSignedPlainDecimal`/`parsePlainDecimalToMinor`) and `core/designsystem/component/LedgerAmountInputField.kt`.

Separately, the user observed transaction history appearing to start "June 26th" despite selecting "This Month" (which should mean June 17, confirmed exactly via `import_summary.json`'s `windowStartMillis` and a June 18 SMS visible in the log). This is very likely just `DashboardViewModel`'s "Recent Activity" list being capped at the 20 most recent transactions (`observeRecentTransactions(20)`) — the true import boundary is correct; June 26 is just the oldest of the visible 20, not the actual cutoff. Not fully confirmed (no per-transaction date export exists in the diagnostic bundle to prove it directly) — flagged as the likely explanation, not a fixed conclusion; not changed.

Also fixed from a real screenshot: `DashboardScreen.TotalBalanceSection`'s month-over-month change badge (`"-56%"` in the screenshot) was rendering one character per line — the balance `Text` and the badge shared a `Row` with no width floor on the badge, so a wide balance string (real device data, not short preview text) left the badge almost no room and Compose's default text wrapping stacked it vertically. Fixed by stacking the badge below the balance instead of beside it, removing the horizontal contention entirely regardless of number width.

## Diagnosed, not a bug — Merchant Intelligence "UNKNOWN" categories (Part 6, 2026-07-17)

`MerchantRegistry` (`feature/merchant/MerchantRegistry.kt`, in the frozen list above) is a deliberately small, curated allowlist of ~19 major UAE/India chains (Amazon, Careem, Carrefour, Noon, Etisalat, etc.). `MerchantResolver` works exactly as documented — "deterministic, nothing invented" — returning `Unresolved` → `"UNKNOWN"` category for anything not in that list. Real diagnostic evidence: "Al Madina Fresh Mart," "Cars Taxi," "Akko Restaurant Llc," etc. are small independent local businesses that were never in scope for this registry; it isn't bypassed or failing.

**Follow-up built (2026-07-17)**: new `feature/merchant/GenericCategoryKeywords.kt` — a small, separate, additive keyword→category fallback (TAXI/CAB→Transport, BAQALA/MART/GROCERY/HYPERMARKET→Groceries, RESTAURANT/GRILL/KITCHEN/CAFE/BAKERY→Dining, PETROL/FUEL/GAS STATION→Fuel, PHARMACY/CLINIC/HOSPITAL→Health), consulted only in `GetFinancialAnalyticsUseCase` (both call sites) when `MerchantResolver` already returned `Unresolved`. Deliberately does NOT touch `MerchantResolver.kt`/`MerchantRegistry.kt` (still frozen, still untouched) and never overrides a resolved brand's own category — still "no AI," still returns `null`/"UNKNOWN" for anything that matches no keyword either, so nothing is invented.

## Test suite status

All unit tests pass, including after the Dashboard sign-bug fix and the import-onboarding rebuild above (2026-07-17). The 8th previously-failing test (`ExtractionEngineTest`) is gone, not fixed-in-place — it tested the now-deleted dead extraction pipeline (see "Frozen / protected" above). `compileReleaseKotlin` also verified separately (debug passing is not proof of release, per the gotcha above). Run `./gradlew testDebugUnitTest` to confirm current state before trusting this section — it may drift as work continues.

## Deferred, not forgotten

- Git commit hash in `AppInfoCollector`/App Info: needs a `build.gradle.kts` `buildConfigField` change, deliberately not attempted without the ability to compile-test it.
- `app/release/*.apk` and baseline profile `.dm` files got committed to git by accident at some point — should be `.gitignore`'d, not yet cleaned up.
- Git history on `feature/ldl-foundation` is squashed/mixed (multiple logical changes landed in single commits, sometimes combined with unrelated UI work) — don't assume one commit equals one atomic change when reading `git log`.
- Merchant category keyword-fallback layer (Part 6 follow-up above) — not built, scoped but deliberately deferred.
- The new onboarding range-selection UI (`ImportRangeSelectionScreen`, `LedgerDateRangeCalendar`) has never been run on an emulator or device — compile/tests only. Visually verify before shipping.
